# Install Helper

This folder contains guidance for packaging and installer-style deployment.

Use this package structure for installers and portable releases.

## Where to place files
- `lua/pomorodo.lua` -> selected in OBS Scripts.
- `dock/index.html` -> used as a Custom Browser Dock URL.
- `docs/` -> user-facing quickstart and installation documentation.
